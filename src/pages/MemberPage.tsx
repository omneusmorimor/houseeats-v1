import React,{useEffect,useMemo,useState}from"react";
import"../member-polish.css";
import{supabase}from"../lib/supabase";
import NotificationCenter,{type HouseNotification}from"../components/NotificationCenter";
import{addDays,memberWindow,toISODate}from"../lib/calendar";
const allergens=["Milk","Eggs","Wheat","Soy","Peanuts","Tree Nuts","Fish","Shellfish","Sesame"];const mealOrder=["lunch","dinner"];
type Props={user:any;profile:any};type Meal={id:string;meal_date:string;meal_type:string;name:string;description:string;allergens:string[]};type Tab="home"|"menu"|"allergies"|"late"|"notifications"|"profile";
const sortMeals=(items:Meal[])=>[...items].sort((a,b)=>{const d=a.meal_date.localeCompare(b.meal_date);if(d)return d;const ai=mealOrder.indexOf(a.meal_type.toLowerCase()),bi=mealOrder.indexOf(b.meal_type.toLowerCase());return(ai<0?99:ai)-(bi<0?99:bi)});
export default function MemberPage({user,profile}:Props){const[tab,setTab]=useState<Tab>("home"),[meals,setMeals]=useState<Meal[]>([]),[myAllergies,setMyAllergies]=useState<string[]>([]),[rsvps,setRsvps]=useState<Record<string,boolean>>({}),[late,setLate]=useState<string[]>([]),[notifications,setNotifications]=useState<HouseNotification[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(""),[calendarNow]=useState(()=>new Date());const{start:rangeStart}=memberWindow(calendarNow),rangeEnd=addDays(rangeStart,13);
async function load(){setLoading(true);setError("");const{data:menu}=await supabase.from("menus").select("id").eq("active",true).order("created_at",{ascending:false}).limit(1).maybeSingle();if(menu){const{data,error:e}=await supabase.from("meals").select("id,meal_date,meal_type,name,description,allergens").eq("menu_id",menu.id).gte("meal_date",toISODate(rangeStart)).lte("meal_date",toISODate(rangeEnd)).order("meal_date");if(e)setError(e.message);setMeals(sortMeals((data||[]).filter((m:any)=>mealOrder.includes(m.meal_type.toLowerCase()))))}else setMeals([]);const[a,r,l,n]=await Promise.all([supabase.from("member_allergies").select("allergen").eq("user_id",user.id),supabase.from("rsvps").select("meal_id,attending").eq("user_id",user.id),supabase.from("late_plates").select("meal_id").eq("user_id",user.id).neq("status","cancelled"),supabase.from("notifications").select("id,type,title,message,read,created_at").eq("user_id",user.id).order("created_at",{ascending:false}).limit(50)]);setMyAllergies((a.data||[]).map((x:any)=>x.allergen));setRsvps(Object.fromEntries((r.data||[]).map((x:any)=>[x.meal_id,x.attending])));setLate((l.data||[]).map((x:any)=>x.meal_id));setNotifications((n.data||[])as HouseNotification[]);const firstError=[a.error,r.error,l.error,n.error].find(Boolean);if(firstError)setError(firstError.message);setLoading(false)}
useEffect(()=>{load();const channel=supabase.channel(`member-notifications-${user.id}`).on("postgres_changes",{event:"*",schema:"public",table:"notifications",filter:`user_id=eq.${user.id}`},()=>load()).subscribe();return()=>{supabase.removeChannel(channel)}},[user.id,rangeStart.toISOString(),rangeEnd.toISOString()]);
async function toggleRsvp(meal_id:string){const attending=!rsvps[meal_id];const{error:e}=await supabase.from("rsvps").upsert({user_id:user.id,meal_id,attending,updated_at:new Date().toISOString()},{onConflict:"user_id,meal_id"});if(!e)setRsvps(x=>({...x,[meal_id]:attending}));else setError(e.message)}async function toggleAllergy(a:string){if(myAllergies.includes(a)){const{error:e}=await supabase.from("member_allergies").delete().eq("user_id",user.id).eq("allergen",a);if(!e)setMyAllergies(x=>x.filter(v=>v!==a));else setError(e.message)}else{const{error:e}=await supabase.from("member_allergies").insert({user_id:user.id,allergen:a});if(!e)setMyAllergies(x=>[...x,a]);else setError(e.message)}}async function requestLate(meal_id:string){if(late.includes(meal_id))return;const{error:e}=await supabase.from("late_plates").insert({user_id:user.id,meal_id,status:"requested"});if(!e)setLate(x=>[...x,meal_id]);else setError(e.message)}async function read(id:string){const{error:e}=await supabase.from("notifications").update({read:true}).eq("id",id).eq("user_id",user.id);if(!e)setNotifications(x=>x.map(n=>n.id===id?{...n,read:true}:n))}async function readAll(){const{error:e}=await supabase.from("notifications").update({read:true}).eq("user_id",user.id).eq("read",false);if(!e)setNotifications(x=>x.map(n=>({...n,read:true})))}
const unread=notifications.filter(n=>!n.read).length,today=toISODate(new Date()),todays=sortMeals(meals.filter(m=>m.meal_date===today)),upcoming=sortMeals(meals.filter(m=>m.meal_date>=today)),nextMeal=upcoming[0],days=useMemo(()=>Array.from({length:14},(_,i)=>addDays(rangeStart,i)),[rangeStart]);
const mealRow=(m:Meal)=>{const danger=(m.allergens||[]).some(a=>myAllergies.includes(a));return <article className="he-meal"key={m.id}><div className="he-meal-date"><span>{new Date(`${m.meal_date}T12:00:00`).toLocaleDateString(undefined,{weekday:"short"})}</span><b>{new Date(`${m.meal_date}T12:00:00`).getDate()}</b></div><div className="he-meal-content"><div className="he-meal-type">{m.meal_type}</div><h3>{m.name}</h3>{m.description&&<p>{m.description}</p>}{danger&&<div className="he-alert">⚠ Contains {myAllergies.filter(a=>(m.allergens||[]).includes(a)).join(", ")}</div>}<div className="he-meal-actions"><button className={rsvps[m.id]?"he-going":"he-primary"}onClick={()=>toggleRsvp(m.id)}>{rsvps[m.id]?"✓ You're going":"RSVP"}</button><button className="he-secondary"onClick={()=>requestLate(m.id)}disabled={late.includes(m.id)}>{late.includes(m.id)?"Late plate requested":"Late plate"}</button></div></div></article>};const go=(x:Tab)=>setTab(x);
const navItems:[Tab,string,string][]=[["home","🏠","Home"],["menu","🍽️","Menu"],["allergies","⚠️","Allergies"],["late","🥡","Late Plate"],["notifications","🔔","Alerts"]];
const displayName=profile?.full_name?.trim()||"Member";const firstName=displayName.split(/\s+/)[0]||"Member";const hour=new Date().getHours();const greeting=hour<12?"Good morning":hour<18?"Good afternoon":"Good evening";
return <main className="he-member"><header className="he-top"style={{padding:"2px 0 0",marginBottom:18,alignItems:"center"}}><button onClick={()=>go("home")}aria-label="HouseEats home"style={{display:"flex",alignItems:"center",gap:9,padding:0,border:0,background:"transparent",textAlign:"left"}}><span aria-hidden="true"style={{width:36,height:36,borderRadius:11,display:"grid",placeItems:"center",background:"#18324a",color:"#fff",fontSize:18,boxShadow:"0 4px 12px rgba(24,50,74,.10)"}}>🍽️</span><span><span className="he-brand"style={{display:"block",fontSize:15,lineHeight:1,letterSpacing:"-.5px"}}>HOUSE<span>EATS</span></span><span style={{display:"block",marginTop:4,fontSize:10,color:"#87929a",fontWeight:600}}>{greeting}, {firstName}</span></span></button><div className="he-top-actions"><button aria-label={`Notifications${unread?`, ${unread} unread`:""}`}onClick={()=>go("notifications")}>🔔{unread>0&&<i>{unread}</i>}</button><button aria-label="Profile"onClick={()=>go("profile")}style={{background:"#18324a",color:"#fff",borderColor:"#18324a",fontSize:14}}>👤</button></div></header>{error&&<div className="he-error">{error}</div>}{loading?<div className="he-loading">Loading your meals…</div>:<>{tab==="home"&&<><section className="he-tonight"><div className="he-tonight-copy"><span>{todays.length?"TODAY":"UP NEXT"}</span><h1>{nextMeal?.name||"No meal posted yet"}</h1><p>{nextMeal?`${nextMeal.meal_date===today?"Today":nextMeal.meal_date} · ${nextMeal.meal_type}`:"Check back when the kitchen posts the menu."}</p></div>{nextMeal&&<div className="he-plate"aria-hidden="true">🍽️</div>}{nextMeal&&<div className="he-tonight-buttons"><button className="he-light-primary"onClick={()=>toggleRsvp(nextMeal.id)}>{rsvps[nextMeal.id]?"✓ You're going":"RSVP"}</button><button className="he-light-secondary"onClick={()=>requestLate(nextMeal.id)}disabled={late.includes(nextMeal.id)}>{late.includes(nextMeal.id)?"Late plate requested":"Need a late plate?"}</button></div>}</section>{myAllergies.length>0&&<button className="he-allergy-strip"onClick={()=>go("allergies")}><span>⚠️</span><div><b>Dietary alerts are on</b><small>{myAllergies.join(" · ")}</small></div><strong>›</strong></button>}<section className="he-section"><div className="he-section-head"><div><span>UPCOMING</span><h2>This week</h2></div><button onClick={()=>go("menu")}>See all</button></div><div className="he-feed">{upcoming.slice(0,5).map(mealRow)}</div></section><div className="he-quick"><button onClick={()=>go("late")}><b>🥡</b><span>Late plate</span></button><button onClick={()=>go("allergies")}><b>⚠️</b><span>Allergies</span></button><button onClick={()=>go("menu")}><b>📅</b><span>Full menu</span></button></div></>}{tab==="menu"&&<section className="he-page"><div className="he-page-title"><span>HOUSE MENU</span><h1>Meals</h1><p>Your next two weeks.</p></div><div className="he-days">{days.map(d=>{const ds=toISODate(d),ms=sortMeals(meals.filter(m=>m.meal_date===ds));return <section className={ds===today?"he-day today":"he-day"}key={ds}><div className="he-day-head"><b>{d.toLocaleDateString(undefined,{weekday:"long"})}</b><span>{d.toLocaleDateString(undefined,{month:"short",day:"numeric"})}</span></div>{ms.length?ms.map(mealRow):<p className="he-no-meal">No meal posted</p>}</section>})}</div></section>}{tab==="allergies"&&<section className="he-page"><div className="he-page-title"><span>YOUR PROFILE</span><h1>Dietary alerts</h1><p>Tell the kitchen what you need to avoid.</p></div><div className="he-allergy-list">{allergens.map(a=><label key={a}><input type="checkbox"checked={myAllergies.includes(a)}onChange={()=>toggleAllergy(a)}/><span>{a}</span><i>{myAllergies.includes(a)?"✓":""}</i></label>)}</div></section>}{tab==="late"&&<section className="he-page"><div className="he-page-title"><span>MEAL SERVICE</span><h1>Late plate</h1><p>Request a plate when you'll miss normal dinner time.</p></div>{late.length?<div className="he-request-list">{late.map(id=>{const m=meals.find(x=>x.id===id);return <div className="he-request"key={id}><div><span>{m?.meal_type}</span><b>{m?.name||"Meal"}</b><small>{m?.meal_date}</small></div><em>Requested</em></div>})}</div>:<div className="he-empty">No late plates right now.<br/><button onClick={()=>go("menu")}>Browse meals</button></div>}</section>}{tab==="notifications"&&<section className="he-page"><div className="he-page-title"><span>HOUSEEATS</span><h1>Alerts</h1><p>{unread?`${unread} unread notification${unread===1?"":"s"}`:"You're all caught up."}</p></div><NotificationCenter notifications={notifications}onRead={read}onReadAll={readAll}/></section>}{tab==="profile"&&<section className="he-page"><div className="he-page-title"><span>ACCOUNT</span><h1>{profile?.full_name||"Member"}</h1><p>{profile?.email||user.email}</p></div><div className="he-profile-links"><button onClick={()=>go("allergies")}>Dietary alerts <span>›</span></button><button onClick={async()=>{await supabase.auth.signOut()}}>Sign out <span>›</span></button></div></section>}</>}<nav className="he-bottom-nav"aria-label="Member navigation">{navItems.map(([key,icon,label])=><button key={key}className={tab===key?"active":""}onClick={()=>go(key)}aria-current={tab===key?"page":undefined}><span className="he-nav-icon">{icon}</span><span>{label}</span>{key==="notifications"&&unread>0&&<i>{unread}</i>}</button>)}</nav></main>}
